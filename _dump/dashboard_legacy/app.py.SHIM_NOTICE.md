# Legacy `dashboard/app.py` — deprecated

The **canonical v3 dashboard** is now `v3_arcadia/85_infinite_baths/dashboard.py`. It aggregates every phase JSON (R1 through R6) in one Streamlit app.

```bash
# Run the current dashboard
streamlit run v3_arcadia/85_infinite_baths/dashboard.py
```

This `dashboard/` directory contains the v2 analyst dashboard. Kept for reference; not part of the v3.0-arcadia release.

To migrate an existing bookmark, point to the v3 dashboard.
