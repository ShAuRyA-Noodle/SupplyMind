# Slim runtime for Damocles v3 API (FastAPI)
# Excludes training-only deps (sb3, transformers, chronos, timesfm, tabpfn)
# Embedders, reranker, ONNX inference only.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
numpy>=1.26
requests>=2.31

# Embedder (mxbai 1024-d, ~1.3 GB safetensors)
sentence-transformers>=2.7.0
torch>=2.2.0

# ONNX Runtime for PPO policy
onnxruntime>=1.27.0

# Forecast runtime (Chronos inference only, no training)
chronos-forecasting>=1.1.0

# For /rag corpus loading
pickle-mixin>=1.0.2
